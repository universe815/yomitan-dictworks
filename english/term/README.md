# English term dictionaries

Term dictionaries are grouped by translation direction:

- `en-zh/`: English → Chinese;
- future pairs may use folders such as `en-en/`, `en-ja/`, and `en-fr/`.

Every dictionary page records its build status, public download, update manifest,
and third-party ownership notice.
